# 📱 手機訪問指南

本應用已完全適配行動設備，支援在手機和平板上使用。

## 🚀 快速開始

### 步驟 1: 啟動後端服務

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

後端將運行於 `http://0.0.0.0:8000`

### 步驟 2: 啟動前端服務

```bash
cd frontend
npm install
npm run dev
```

前端將運行於 `http://0.0.0.0:5173`

### 步驟 3: 在手機上訪問

#### 3a. 查找電腦 IP 地址

**macOS/Linux:**
```bash
ifconfig | grep "inet " | grep -v "127.0.0.1"
```

**Windows:**
```bash
ipconfig
```

尋找以下格式的地址：`192.168.x.x` 或 `10.x.x.x`

#### 3b. 配置前端 API 地址

在 `frontend/` 目錄下建立 `.env.local` 檔案：

```env
VITE_API_URL=http://YOUR_COMPUTER_IP:8000
```

例如：
```env
VITE_API_URL=http://192.168.1.100:8000
```

#### 3c. 在手機瀏覽器中訪問

```
http://YOUR_COMPUTER_IP:5173
```

例如：`http://192.168.1.100:5173`

## ✅ 手機功能支援

| 功能 | 支援 | 備註 |
|------|------|------|
| 輸入血壓值 | ✅ | 觸屏友好的數字輸入 |
| 輸入心率 | ✅ | 可選欄位 |
| 添加備註 | ✅ | 支援長文本輸入 |
| **拍攝照片** | ✅ | 使用手機相機 |
| 選擇檔案 | ✅ | 從相冊選擇圖片 |
| 查看記錄列表 | ✅ | 響應式列表設計 |
| 展開詳細信息 | ✅ | 觸屏友好 |
| 查看照片 | ✅ | 全屏顯示 |
| 刪除記錄 | ✅ | 確認對話框 |

## 🔐 相機權限

### iOS
1. 開啟應用時會提示請求相機權限
2. 選擇「允許」以授予相機訪問
3. 可在「設定 > 隱私 > 相機」中檢查權限

### Android
1. 開啟應用時會提示請求相機權限
2. 點擊「允許」或「允許此次」
3. 可在「設定 > 應用和通知 > 權限」中檢查權限

## 🌐 網絡連接

### 同一 Wi-Fi 網絡

手機和電腦需連接到**同一個 Wi-Fi 網絡**。

### 通過 USB 連接（需要進階設置）

如果需要通過 USB 調試連接，請使用 `adb` 轉發端口：

```bash
adb forward tcp:5173 tcp:5173
adb forward tcp:8000 tcp:8000
```

然後在手機瀏覽器訪問：`http://localhost:5173`

## 📱 推薦瀏覽器

- **iOS**: Safari, Chrome
- **Android**: Chrome, Firefox

## 🛠️ 常見問題

### Q: 手機無法連接到應用

**A:** 檢查以下項目：
1. 手機和電腦在同一 Wi-Fi 網絡
2. 後端和前端都已啟動
3. IP 地址配置正確
4. 防火牆未阻止連接

### Q: 相機拍攝失敗

**A:**
1. 確保已授予相機權限
2. 使用「選擇檔案」作為備選方案
3. 嘗試重新載入頁面

### Q: 照片上傳失敗

**A:**
1. 檢查網絡連接
2. 確認後端服務正在運行
3. 檢查檔案大小（建議 < 5MB）

### Q: 應用在手機上顯示不完整

**A:**
1. 嘗試旋轉螢幕
2. 調整瀏覽器縮放級別
3. 清除瀏覽器緩存

## 🎯 最佳實踐

1. **使用 iOS Safari** - 相機功能支援最佳
2. **保持 Wi-Fi 連接穩定** - 避免網絡中斷
3. **定期備份記錄** - 導出數據以防數據丟失
4. **明亮環境拍攝** - 提高照片質量

## 📊 測試清單

在手機上完全測試前，請確保：

- [ ] 能訪問應用頁面
- [ ] 能输入血壓值
- [ ] 能拍攝照片
- [ ] 能上傳記錄
- [ ] 能查看記錄列表
- [ ] 能刪除記錄
- [ ] 能查看照片

## 🚀 部署到遠程服務器

對於更長期的使用，建議將應用部署到真實服務器：

1. 使用 Nginx 或 Apache 部署前端
2. 使用 Gunicorn 或 Waitress 部署後端
3. 配置 SSL 證書以支援 HTTPS
4. 設置防火牆和訪問控制

詳見主 README.md 的部署章節。
